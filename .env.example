# OpenAI API key — can also be set via the UI Settings panel
OPENAI_API_KEY=sk-your-api-key-here

# Maximum upload file size in megabytes
MAX_FILE_SIZE_MB=50

# Document chunking defaults
CHUNK_SIZE=1000
CHUNK_OVERLAP=200
